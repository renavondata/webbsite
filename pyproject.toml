[project]
name = "webbsite"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "flask",
    "psycopg2-binary",
    "gunicorn",
    "python-dotenv",
]

[dependency-groups]
dev = [
    "requests>=2.32.5",
]
